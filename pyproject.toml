[project]
name = "polars-benchmark-modal"
version = "0.1.0"
description = "Run the Polars PDS-H benchmark on Modal"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [{ name = "Modal Labs, Inc." }]
requires-python = ">=3.10"
dependencies = ["modal"]

[project.optional-dependencies]
charts = ["matplotlib"]

[tool.ruff]
line-length = 88
target-version = "py310"
